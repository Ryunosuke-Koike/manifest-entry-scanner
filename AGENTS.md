# AGENTS.md

## 0. 二段階の起動

このリポジトリは、アプリの要件を詳しく整理し、別の場所へコピーできる自己完結した実装用プロジェクトを生成するためのスターターである。

### 第1段階: 開発準備

利用者が次の起動語を独立した指示として入力した場合、`.codex/skills/start-app-development/SKILL.md` を優先して読み、詳細な要件確認を開始する。起動語について質問、引用、説明しているだけの場合は開始しない。

- `開発スタート`
- `アプリ開発スタート`
- `Codex開発スタート`
- `START APP DEV`
- `/start-app-dev`

起動語の後ろに続く文章は、今回の開発テーマまたは追加条件として扱う。

この段階ではアプリ本体を実装しない。詳細なGrill-me、要件確定、計画、技術選定、実装用プロジェクトディレクトリの生成までを行う。

### 第2段階: 実装

生成された実装用プロジェクトを人が任意の場所へコピーした後、そのディレクトリ内で次のいずれかを独立した指示として入力すると実装を開始する。

- `実装スタート`
- `実装開始`
- `アプリ実装スタート`
- `START IMPLEMENTATION`
- `/start-implementation`

単に文章中に「実装」が含まれるだけでは開始しない。実装用プロジェクト内の `.codex/skills/start-implementation/SKILL.md` に従う。

### 次の案件へ進む前の初期化

利用者が次の起動語を独立した指示として入力した場合、`.codex/skills/reset-project-state/SKILL.md` を読み、安全な案件初期化フローを開始する。起動語について質問、引用、説明しているだけの場合は開始しない。

- `初期状態に戻す`
- `案件初期化`
- `次の開発準備`
- `/reset-project-state`

初期化は、完了案件のPRマージ、スターター外へのコピー、コピー内容の一致、Git同期を確認した後に行う。生成物は直接削除せず復旧用に退避し、スターター機能、運用ルール、開発ログ、Git履歴を維持する。

## 1. 目的

このリポジトリでは、Codexが非エンジニアの依頼者と協力しながら、実装前の不明点を解消し、実装に必要な情報とルールを備えた自己完結型プロジェクトを生成します。
生成後のプロジェクトでは、価値を確認できる「動く最小版」から段階的に実装します。

## 2. 全エージェント共通ルール

1. 作業前に `PROJECT_BRIEF.md`、`plans/MASTER_PLAN.md`、`tasks/TASKS.md` を確認する。
2. `.codex/skills/start-app-development/references/requirements-interview.md` に従い、Grill-me形式で質問を1問ずつ行う。各質問には推奨回答と理由を付け、回答に応じて関連する論点を掘り下げる。
3. 質問は初心者にもわかる言葉を使い、専門用語には短い説明を付ける。
4. 技術・サービス・ライブラリを提案するときは、以下を必ず説明する。
   - 何をするものか
   - なぜ必要か
   - 他の選択肢
   - 費用の有無
   - 運用上の注意
5. 一度に大規模な完成形を作らず、段階的なゴールを設定する。
6. 各段階で「確認できる動作」を必ず定義する。
7. 実装後は最低2種類のレビューを受ける。
8. レビュー結果は統括エージェントが整理し、採用・保留・却下を判断する。
9. 修正後はテスト結果と変更内容を記録する。
10. 秘密情報、APIキー、パスワードをソースコードへ直接書かない。
11. このスターターでの要件整理完了後、`generated-projects/<project-slug>/` に実装用プロジェクトを生成する。同名ディレクトリを無断で上書きしない。
12. 生成するプロジェクトは親ディレクトリに依存しない自己完結型とし、要件、計画、タスク、判断、レビュー、ログ、Git運用、役割、起動スキル、技術に応じたソース・テスト構成を含める。
13. 生成した実装用プロジェクトでは、`PROJECT_ROOT.md` と `AGENTS.md` がある現在のディレクトリをルートとして検証し、以後の成果物をそのルート内に保存する。端末固有の絶対パスは追跡ファイルへ記録しない。
14. 実装開始時はGit管理を開始し、以後の変更履歴を残す。
15. リモートリポジトリは自動作成しない。人が作成したリポジトリURLを確認してから接続し、接続結果を検証する。
16. 変更や新機能の作業前に、直前の正常な状態がコミット済みか確認し、リモートへプッシュして基準点を保存する。未整理の変更を無断でまとめてコミットしない。
17. 影響の大きい作業は、作業前にIssueを作成し、そのIssueに対応するブランチで作業する。完了後はテスト結果を記載したPull Request（PR）を作成する。
18. PRは人がレビューしてマージする。Codexは、明示的に依頼されない限りマージや自動マージの設定を行わない。
19. コード内コメント、コミットメッセージ、Issue、PR、レビューへの返信、開発ログは、固有名詞やコマンドなどを除いて日本語で記述する。
20. 作業内容、確認結果、失敗、原因、修正、次回への改善を `logs/DEVELOPMENT_LOG.md` に記録する。
21. 統括エージェントは作業開始前と統括レビュー時に開発ログを確認し、同じ失敗や手戻りを防ぐため、必要に応じて計画、タスク、開発手順を更新する。
22. 次の案件へ進むときは `.codex/skills/reset-project-state/SKILL.md` に従い、案件固有状態だけを初期化する。未コミット変更、未同期ブランチ、未マージPR、コピー未確認の状態では初期化しない。

Git・Issue・ブランチ・PRの具体的な運用は `docs/GIT_WORKFLOW.md` に従う。

## 3. 開発の優先順位

1. 動くこと
2. 依頼者が理解・確認できること
3. 壊れにくいこと
4. 保守しやすいこと
5. 高度な最適化や美しい設計

## 4. 標準ワークフロー

1. 詳細Grill-meによる要件確認
2. 要件要約を人が確認
3. 段階的ゴールの提案
4. 技術候補の説明と人による選択
5. 最小実装計画とタスクの作成
6. 自己完結した実装用プロジェクトディレクトリの生成
7. ファイルツリー、内部参照、必須文書の検証
8. 人が生成ディレクトリを任意の場所へコピー
9. コピー先で実装起動語を入力
10. プロジェクトルートを確定
11. Git初期化・人が提示したリモートへの接続
12. 作業前のコミット・プッシュ
13. 影響が大きい場合はIssue作成・ブランチ作成
14. 実装、テスト、二重レビュー、統括レビュー
15. 影響が大きい場合はPR作成
16. 人による確認・PRのマージ
17. 開発ログと手順の更新
18. 次段階へ進む
19. 案件完了後、人の指示により安全な案件初期化を行う
20. 初期化PRを人がレビュー・マージしてから次の「開発スタート」へ進む

## 5. 必須成果物

要件整理段階の最終成果物は、`generated-projects/<project-slug>/` に生成された自己完結型の実装用プロジェクトディレクトリである。最低限、次を含める。

- `AGENTS.md`
- `PROJECT_ROOT.md`
- `PROJECT_BRIEF.md`
- `plans/MASTER_PLAN.md`
- `tasks/TASKS.md`
- `decisions/DECISION_LOG.md`
- `reviews/REVIEW_CONSERVATIVE.md`
- `reviews/REVIEW_PRAGMATIC.md`
- `reviews/REVIEW_LEAD.md`
- `logs/DEVELOPMENT_LOG.md`
- `.codex/skills/start-implementation/SKILL.md`
- 採用技術に応じたソース・テストのディレクトリと初期設定（アプリ機能の実装は含めない）
- 実装後に使用するテストまたは確認手順

## 6. エージェントの役割

- 統括: `agents/lead-agent.md`
- 要件整理: `agents/product-agent.md`
- 実装: `agents/builder-agent.md`
- 慎重派レビュー: `agents/conservative-reviewer.md`
- 実用派レビュー: `agents/pragmatic-reviewer.md`
- 初心者向け説明: `agents/explainer-agent.md`

## 7. 完了条件

要件整理と引き渡しを完了にできるのは、以下を満たした場合だけです。

- 重要な未確定事項が解消され、要件要約を人が確認している
- 技術構成と段階的ゴールが決定している
- 実装用プロジェクトが `generated-projects/<project-slug>/` に生成されている
- 生成物が親ディレクトリに依存せず、コピー後も自己完結している
- 必須ファイルと内部参照が検証されている
- コピー後の実装起動語と操作方法を人へ案内している

生成後の実装タスクを完了にできるのは、以下を満たした場合だけです。

- 実装が存在する
- 動作確認方法がある
- 確認結果が記録されている
- 重大なレビュー指摘が解消されている
- `tasks/TASKS.md` が更新されている
- 作業内容と確認結果が `logs/DEVELOPMENT_LOG.md` に記録されている
- 影響の大きい作業ではIssueとPRが作成され、人のレビュー待ちになっている
