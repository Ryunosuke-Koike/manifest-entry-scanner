# 統括レビュー

## 対象

Issue #3のGoal 1初期実装（G1-005〜G1-008）と、慎重派・実用派レビュー。

## 結論

Goal 1の試作として採用する。担当者のサーバー側再検証、11桁番号・7票種の入力制限、LockService内の重複確認、Canvas画像をサーバーへ渡さない境界を確認した。自動テストは成功している。実機とApps Script接続が未確認のため、Goal 1は未完了のままG1-007をブロック、G1-008を作業中とする。

## 採用する指摘

- 実画像、Spreadsheet ID、OAuthトークンをGitへ保存しない。
- OCR候補なしを票種Aへ補完せず、手動選択を要求する。
- クライアント値を信頼せず、有効な担当者一覧とサーバー側で照合する。
- 初回Worker準備時間と撮影後処理時間を分けて測定する。
- カメラ互換性、アカウントメール取得、画像非送信を実環境の証拠で確認する。

## 保留する指摘

- 7票種×10枚の精度・速度検証、連続登録、取消、セッション復元はGoal 2へ保留する。
- 保存期間は管理担当者の回答待ちとする。
- 票種の固定範囲とOCR閾値は見本画像の実測前に変更しない。

## 次のアクション

管理者からApps Scriptプロジェクト、実験用Spreadsheet、7票種の見本画像、iPhone/Android実機を受領する。`docs/DEPLOYMENT.md`に従ってデプロイし、G1-007〜G1-011を順に確認してログへ実測値を記録する。
